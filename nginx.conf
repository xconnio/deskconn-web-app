server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;

    location /app/ {
        try_files $uri $uri/ /app/index.html;
    }

    location = / {
        return 301 $scheme://$http_host/app/;
    }
}
